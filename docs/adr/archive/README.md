# Superseded architecture decisions

This directory preserves decisions from the broader design that preceded ADR-017. These records are retained for audit and historical context. They are superseded, are not part of the current product contract, and must not guide implementation.

The accepted decisions in the parent directory and the numbered specification define current behavior.
