# M3 production-readiness assessment

- Assessment opened: 2026-09-06
- Repository and live GitHub baseline: `db12c51966a8aa432c3ecfeb3e98377087b84a25`
- Current disposition: `incomplete` pending publication of the final source
  candidate and its required CI; the L-54 live exercise is `passed`
- Deployment under review: persistent dev in `us-east-1`; one environment, one
  AWS account, one Slack destination, four public feeds, three services, four
  risk rules, and a reviewed arrival envelope of 300 delivery requests per
  hour globally and per destination
- Reviewed input SHA-256: `config/dev.yaml`
  `66545778ea430d4abb7cb27bd491e455a8719827be3c49e5cb33ffa0645f0bc1`;
  `infra/central/deployment.yaml`
  `785bc448eb68292fccf3e78cc549410a5a016ba8e7fb1a177b7a3729b8dd8122`

This table starts L-43 before further downtime. A reuse decision means the
named evidence still answers the requirement at its stated boundary. On
2026-09-06 authorized L-54 stages deployed the genuine successor across all
five functions, passed its identity refusals and one bounded shadow sample,
restored the complete runtime topology, and observed one natural healthy
scheduled cycle. The later application-only exercise passed its predecessor
and forward transitions, both bounded shadow checks, stopped-state comparison,
normal restoration, scheduled observation, and final Terraform convergence.

| Requirement | Exact baseline evidence | Intervening change checked | Reuse, targeted rerun, or blocker | Remaining outcome |
| --- | --- | --- | --- | --- |
| Reviewed scope and policy inputs | L-40 issue #145 and `docs/evidence/production-policy.md` SHA-256 `4f6a4ad8ea2d1de27fd9fa499848a227cbca0c99ec458ce78d3895c0b5f89b8c` select the input hashes and the 4/3/4 policy. | M2 and M3 changed runtime, packaging, recovery, IAM, and alarms. The reviewed `config/dev.yaml` and `infra/central/deployment.yaml` inputs retained SHA-256 values `66545778…` and `785bc448…`. | Reuse the selection and hashes. Do not project repository ceilings onto the deployment. | Resolved for the exact one-environment, one-destination, four-feed, three-service, four-rule deployment and its 300-delivery/hour envelope. |
| Repository contracts, schemas, identities, and clean build | L-49 #186 independently binds recovery hashes. L-52 #189 / PR #195 at `db12c519…` passed repository quality, security, references, and the Ubuntu 24.04 Python 3.12 package import job (Actions run `34049165126`). | This L-53 preparation changes package validation, artifact guards, quiescence, tests, specifications, and runbooks. | Rerun focused inversions and the full repository gates on the final source. A new package build must use the accepted target. | Passing checks on the final commit and CI identity. |
| Corpus quality and configured pair disposition | L-40: 47 items, 32 historical and 15 synthetic, 29 true positives, no false positives or false negatives, global precision and recall 1.000. Six of 12 configured pairs have no historical positive, four have one, and two have two and seven. | No matcher, corpus, label, service, or risk-rule input changed. ADR-018's 2026-09-06 revision reconciles the governing text with the accepted thin-data disposition. | Reuse the measured result. Do not extend sampling to obtain positives. | Criterion resolved and passed for the reviewed inputs; rerun evaluator only if final inputs change. |
| Feed acquisition, normalization, and freshness | M1 #137 records a 12-message persistent-dev cohort. L-42 #147 evidence SHA-256 `80ef235d43891895fdb92f90975c9c285001363dbbefe042fb1ebfb0cd3b7fcf` records three four-feed samples, each with 240 normalized items and eight candidates. | Full-text summary bounding changed after M1; L-42 exercised that production acquisition and normalization path. L-52 changed package construction, not this runtime logic. The L-54 predecessor and restored-successor samples each fetched all four feeds, normalized 240 items, and produced the same eight candidate identities. The final natural watcher opportunity invoked once with zero errors or throttles and a heartbeat. | Reuse L-42 and retain the fixed L-54 samples. Do not repeat them to obtain a positive. | Resolved. |
| Shadow isolation and comparison | L-42 records three valid shadow invocations with equal candidate digest, fixed identity refusals, restricted IAM, and byte-identical durable state before and after. | L-53 changed durable-executor pause and package eligibility, not shadow composition. Current IAM, retry, concurrency, and no-durable-write composition matched. The predecessor and restored-successor invocations each dispatched once and returned the same candidate digest. The L-54 endpoint comparison found both owned tables, raw-snapshot inventory, queues, active pointer, successor packages, and paused controls unchanged. | Reuse the isolation proof and retain the fixed samples and stopped comparison. | Resolved. |
| Configuration rollback and forward restoration | L-42 records both compare-and-swap promotions, compatibility probes, exact pointer read-backs, and forward restoration. Its checked-in evidence record has SHA-256 `80ef235d43891895fdb92f90975c9c285001363dbbefe042fb1ebfb0cd3b7fcf`. On 2026-09-06 the retained restricted manifest recomputed to its recorded SHA-256 `ffdf5cfade61eee19efeabccbc8c62035359607535b7a0a03ae2910a66994854`, and a full checksum pass over every listed file succeeded. | A recorded comparison found no intervening change that invalidates the reused exact-version, release-identity and integrity, compare-and-swap, compatibility-probe, or pointer-read-back claims. L-53's pause change is relevant to L-54 execution safety but does not invalidate the earlier configuration transition; L-54 separately proved that complete pause through exact plans. Read-only plan `9b57f0a…` resolved the current and retained pointers and both releases for the successor application version. Replay preview `50505b10…` resolved the pre-exercise snapshot, retained release, `dev-alerts` route, and all four existing candidate and delivery records without apply. Independent read-back confirmed the active pointer VersionId and ETag were unchanged. | ADR-026's accepted application-only revision reuses the L-42 claim. Reassess any later material difference against the affected claim; unrelated changes do not force another transition, and configuration mutation is never an automatic fallback. | Criterion resolved. Recheck only if a later material difference affects this claim. |
| Application rollback and forward restoration | L-42 ended `incomplete`; no application transition was attempted. The qualified predecessor is `c88b49c8f070f1cb808ac005cbe28b484c14be7b29f50a34e21ef3a7ca85ccbd` / `QXNwt_NBIqp0pNKVFalwbZ72587h.GCc`. The genuine successor is `1ae996cabff5e50d9fe3889c67613f983b58be7c590f2c9f1e9a2ec2cbb65dd1` / `yFAfr5Y8fdEnOatvqVHzXlFej5cXSSTg`. | The predecessor and successor qualifications passed before downtime. Actual-state plans changed only five package selections and two artifact guards in each direction while all durable executors remained stopped. Exact read-backs and bounded shadow checks passed on both selections. One actual-state resume plan restored every recorded control and the seven derived alarms. Final Terraform detailed exit code was zero. | Accept the L-54 public evidence record and restricted manifest. | Resolved; L-54 terminal result is `passed`. |
| Slack destination, credential composition, and one real post | D0 #121 records one real Slack post. M1 #78 and #137 record successful private-destination exercises; #137's 50-message cohort reached `posted`. | Dependency lock moved to boto3/botocore 1.43.83. The complete `src/aws_public_change_feed/worker.py`, including rendering implemented there, is unchanged from M1, as are the Slack runtime, transport, routes, delivery mode, rate input, and one-destination policy. After forward restoration, the package, handler, exact worker role actions, route/release composition, and active secret metadata matched; the secret value was not read. | Reuse the prior destination receipt. The boto patch affects the AWS adapter dependency, while the unchanged Slack HTTPS sender uses the standard library. No material difference calls for another isolated post. | Resolved without a forced Slack post. |
| Capacity, pacing, queue, and recovery worker | M1 #137 created exactly five records per minute for ten minutes: 50 posted requests under the selected 300/hour envelope, with queues drained and alarms healthy. | The worker, dispatcher, pacing, rendering, route, and rate behavior did not change. The final L-54 observation recorded 18 dispatcher and three reconciler invocations with heartbeats, zero errors and throttles, empty queues, and no actionable records. | Reuse the 50-message throughput result. Package provenance and pause controls do not invalidate the measured pacing or throughput claim. Do not repeat the load cohort. | Resolved. |
| Alarms, dashboard, and notification subscription | M1 #78 records operator-confirmed notification receipt and recovery/load alarm behavior. L-41 #146 restored seven recovery-relevant alarms; L-42 restored 28 alarms to `OK`. | Final read-back found one confirmed email subscription matching the reviewed plan, an exact dashboard-body match, all 28 alarms and intended actions, and all alarms at `OK` after the fixed observation. The reverse producer contract test passed, and watcher, dispatcher, and reconciler heartbeats were present with zero errors or throttles. | Retain the current configuration and producer evidence and reuse M1's operator-confirmed receipt. No material difference requires another notification action. | Resolved. |
| DynamoDB backup, restore, and service-recovery boundary | L-41 #146 at `3bf35b74a7420b634b6548ca2fe7ff080a2e8bdd` restored both tables to a shared point 298 seconds behind start, verified full inventories/settings, bound disabled runtimes, rolled back to primaries, restarted on primaries, and deleted four temporary tables. L-49 binds digests; L-50 #185 proves primary deletion protection. | L-53 expanded pause from watcher-only to all four durable executors. Final read-back found both primary tables active, bound by the intended Terraform outputs, protected from deletion, and carrying enabled PITR. No evidence shows service running on restored tables. | Reuse L-41 for data restore, stopped binding, rollback, and restart on primaries. ADR-027's accepted revision makes that limited claim explicit; do not claim four-hour service recovery from corrupted primaries. | Resolved within the accepted stopped-restore boundary. Incident promotion stays outside M3. |
| Retention, historical references, replay, and rollback availability | M2 tools provide preview-bound config/package retirement, source retention, replay, and exact-version handling. The archived L-42 snapshot has 15 posted records and three older package digests. | ADR-029 makes older packages ineligible for the current deploy path unless separately qualified. S3 retention does not make them deployable. The L-54 replay previews resolved the retained snapshot and release, all four existing candidate and delivery records, and one missing response-page marker without apply. Current actionable inventory is empty. Exact predecessor and successor objects remain protected and resolved. | The 15 posted records are not stranded actionable work. Only `c88…` was qualified for L-54. Unavailable exact packages remain unchanged under ADR-020. No blanket replay or deletion exercise. | Resolved. |
| IAM, Terraform drift, operator procedure, and restoration controls | M1/M2/L-41/L-42 read-backs and no-change plans cover their own commits. L-50 records final deletion protection convergence. | Central IAM, artifact guards, concurrency, alarms, recovery controls, package provenance, and runbooks changed after those baselines. L-54 read all seven relevant role-policy sets and found no attached managed policies. Shadow had no DynamoDB, S3 write, queue, or secret action, and its invoker had only `lambda:InvokeFunction`. Saved plans preserved the pause across serials 62 and 63, restored from serial 64, and ended at exact backend VersionId `QrLUsn…` for serial 65. | Use the fresh read-backs and actual-state plans rather than blanket reuse. | Live criterion resolved. Final source publication and required CI remain. |
| Terminal readiness decision and status | L-42 is a completed attempt with terminal result `incomplete`; it is not a passing M3 result. L-54 now records `passed`. | The application proof, affected live checks, restored scheduled operation, and Terraform convergence passed. The source remains uncommitted and required CI has not run for the final candidate. | Successful M3 still requires a supported L-43 `passed` result for this exact deployment and envelope. Do not convert the live L-54 pass into premature milestone closure. | Publish the final source and evidence candidate, pass required CI, then record L-43 and L-44 terminal status in GitHub. |

## Decision gates and live result

1. ADR-018's thin-corpus clarification, ADR-027's limited recovery claim, and
   ADR-026's rollback boundary and application-only L-54 follow-up were
   accepted by the owner on 2026-09-06.
2. Exact retained-byte, source, handler, Linux import, and current five-Lambda
   digest qualification passed read-only on 2026-09-06. No pause occurred.
3. Review and authorize each actual publication, deployment, and saved-plan
   mutation only after its exact identity and terminal restoration path are
   known.
4. The accepted application-only reference preflight passed without pointer or
   replay mutation. Its configuration and replay plan SHA-256 values are
   `9b57f0a096310266d3058a638f7020c8e686491d8b029dceb916617345bb987c`
   and `50505b103ee7e39fb8eb4e55cc10630a260249653f439bfe886a4cdee4b522ce`.
5. [L-54's public record](l54-application-rollback-2026-09-07.md) reports a
   terminal `passed` result. The exact predecessor and successor each passed
   a five-function transition, read-back, retained-reference check, and one
   bounded shadow invocation while all durable execution remained stopped.
6. The stopped endpoint comparison matched both table digests, the complete
   raw-snapshot version inventory, queues, active pointer, successor package,
   and controls. Normal operation then resumed, the fixed scheduled window
   passed, all 28 alarms reached `OK`, and Terraform reported no change.
7. The remaining gate is the final source candidate and its required CI. Until
   that passes and the authorized GitHub status records are reconciled, L-43
   and M3 remain `incomplete` rather than production-ready.
